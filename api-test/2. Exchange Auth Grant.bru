meta {
  name: 2. Exchange Auth Grant
  type: http
  seq: 2
}

post {
  url: http://localhost:8000/login/oauth/token
  body: json
  auth: inherit
}

body:json {
  {
    "code": "auth_code"
  }
}

settings {
  encodeUrl: true
}
